# Shared with sun_overlay and cu_salary - same S3 bucket (named for the
# domain) + CloudFront distribution, each project under its own path prefix.
BUCKET=underflow.dev
CLOUDFRONT_DISTRIBUTION_ID=your-distribution-id
